import { EcosystemFilterPipe } from './ecosystem-filter.pipe';

describe('EcosystemFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new EcosystemFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
